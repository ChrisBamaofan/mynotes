javascript悟道
浏览器打开一个窗口就是一个window对象，js运行时依附的全局环境对象和全局作用域对象。

同一域名下，js加载时最多存在两个线程，
执行js最多有一个线程，一个浏览器中所有的窗口加载的js代码都是通过单线程执行的。但是js有保留字段synchronized字段，是考虑到以后js可以多线程执行时。

dom对象的概念，dom即使对象，又是xml文档，也是模型，比如ctree，各种节点，

DOM绑定事件响应函数的传值，只需要将响应函数的构造函数传给DOM对象的响应事件调用的函数的一个属性就可以了。
